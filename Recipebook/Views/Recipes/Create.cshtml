@model Recipebook.Models.ViewModels.RecipeCreateEditVm

@{
    ViewData["Title"] = "Create";
}

<div class="recipe-form-page">
    <h1>Create New Recipe</h1>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="Recipe.Title" class="form-label fw-bold"></label>
                    <input asp-for="Recipe.Title" class="form-control" />
                    <span asp-validation-for="Recipe.Title" class="text-danger"></span>
                </div>

                <div class="form-check mb-3">
                    <input class="form-check-input" asp-for="Recipe.Private" />
                    <label class="form-check-label fw-bold">
                        @Html.DisplayNameFor(model => model.Recipe.Private)
                    </label>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Categories</label>
                    <select name="SelectedCategories"
                            asp-items="ViewBag.AllCategories"
                            multiple="multiple"
                            class="form-control">
                    </select>
                </div>

                <div class="mb-3">
                    <label asp-for="Recipe.Description" class="form-label fw-bold"></label>
                    <textarea asp-for="Recipe.Description" class="form-control" rows="3"></textarea>
                    <span asp-validation-for="Recipe.Description" class="text-danger"></span>
                </div>

                <h4>Ingredients</h4>
                <div class="ingredients-container">
                    @if (Model.Ingredients.Count > 0)
                    {
                        <div class="ingredient-labels row mb-2 fw-bold">
                            <div class="col">Ingredient</div>
                            <div class="col">Quantity</div>
                            <div class="col">Unit</div>
                        </div>
                    }

                    @for (int i = 0; i < Model.Ingredients.Count; i++)
                    {
                        <div class="ingredient-row mb-3">
                            <div class="row g-2 align-items-center">
                                <div class="col">
                                    <!--<select asp-for="Ingredients[@i].IngredientId" class="form-control" asp-items="ViewBag.AllIngredients">
                                        <option value="">-- Select Ingredient --</option>
                                        </select>-->
                                </div>
                                <div class="col">
                                    <input asp-for="Ingredients[@i].Quantity" class="form-control" type="number" min="1" />
                                </div>
                                <div class="col">
                                    <select asp-for="Ingredients[@i].Unit" class="form-control">
                                        @foreach (var unit in Enum.GetValues(typeof(Unit)))
                                        {
                                            <option value="@unit">@unit</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <button type="button" class="btn btn-danger btn-sm remove-ingredient-btn mt-1">Remove Ingredient</button>
                            <hr />
                        </div>
                    }
                </div>

                <button type="button" class="btn btn-brown btn-sm mb-3 add-ingredient-btn w-50 mx-auto">Add Ingredient</button>

                <div class="mb-3">
                    <label asp-for="Recipe.Directions" class="form-label fw-bold"></label>
                    <textarea asp-for="Recipe.Directions" class="form-control" rows="5"></textarea>
                    <span asp-validation-for="Recipe.Directions" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-center gap-2">
                    <button type="submit" class="btn btn-success btn-lg">
                        <i class="bi bi-plus-circle"></i> Create
                    </button>
                    <a onclick="history.back()" class="btn btn-brown btn-lg">
                        Cancel
                    </a>
                </div>
            </form>

            <div class="ingredient-template d-none">
                <div class="ingredient-row mb-3">
                    <div class="row g-2 align-items-center">
                        <div class="col">
                            <!--<select name="Ingredients[__index__].IngredientId" class="form-control">
                                <option value="">-- Select Ingredient --</option>
                                @foreach (var item in (SelectList)ViewBag.AllIngredients)
                                {
                                    <option value="@item.Value">@item.Text</option>
                                }
                            </select>-->
                            <input hidden name="Ingredients[__index__].IngredientId" value="">
                            <div class="dropdown">
                                <button class="form-control dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false" onclick="fetchIngredients(this)">
                                    Select ingredient...
                                </button>
                                <ul class="dropdown-menu p-2">
                                    <input type="text" class="form-control my-2" placeholder="Search..." />
                                    <li><a class="dropdown-item btn-success" href="#"><b>+</b> Create new ingredient</a></li>
                                    <li id="loadingIngredientsText">Loading ingredients...</li>
                                </ul>
                            </div>
                        </div>
                        <div class="col">
                            <input name="Ingredients[__index__].Quantity" class="form-control" type="number" min="1" />
                        </div>
                        <div class="col">
                            <select name="Ingredients[__index__].Unit" class="form-control">
                                @foreach (var unit in Enum.GetValues(typeof(Unit)))
                                {
                                    <option value="@unit">@unit</option>
                                }
                            </select>
                        </div>
                    </div>
                    <button type="button" class="btn btn-danger btn-sm remove-ingredient-btn mt-1">Remove</button>
                    <hr />
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script>
        let ingredientIndex = @Model.Ingredients.Count;
        let cachedIngredients;
        const container = document.querySelector(".ingredients-container");
        const template = document.querySelector(".ingredient-template").innerHTML;

        function updateLabels() {
            const labelsExist = container.querySelector(".ingredient-labels");
            const hasIngredients = container.querySelectorAll(".ingredient-row").length > 0;

            if (hasIngredients && !labelsExist) {
                const labelRow = document.createElement("div");
                labelRow.className = "ingredient-labels row mb-2 fw-bold";
                labelRow.innerHTML = `<div class="col">Ingredient</div><div class="col">Quantity</div><div class="col">Unit</div>`;
                container.prepend(labelRow);
            } else if (!hasIngredients && labelsExist) {
                labelsExist.remove();
            }
        }

        // Event delegation for add/remove using classes only
        document.body.addEventListener("click", e => {
            if (e.target.matches(".add-ingredient-btn")) {
                container.insertAdjacentHTML('beforeend', template.replace(/__index__/g, ingredientIndex++));
                updateLabels();
            }

            if (e.target.matches(".remove-ingredient-btn")) {
                e.target.closest(".ingredient-row").remove();
                updateLabels();
            }
        });

        // ---------- Ingredient dropdown handling ---------- //

        function promptNewIngredient() {
            // tbd
        }

        function createIngredient() {
            // tbd
        }

        function selectIngredient(element) {
            // tbd
        }

        function populateDropdown(element) {
            // get list of ingredients
            const ul = element.parentElement.querySelector(".dropdown-menu");
            cachedIngredients.forEach(ingredient => {
                // create the elements for the dropdown
                var li = document.createElement("li")
                var a = document.createElement("a")
                a.className = "dropdown-item"
                a.href = "#"
                a.textContent = ingredient.name;
                a.dataset.id = ingredient.id;
                li.appendChild(a)
                ul.appendChild(li)
            });
            element.removeAttribute("onclick");
            ul.querySelector("#loadingIngredientsText").remove();
        }

        async function fetchIngredients(element) {
            console.log("fetchIngredients called")
            // if cachedIngredients already exists, populate dropdown
            if (cachedIngredients) 
            { 
                populateDropdown(element); 
                return;
            }

            const dropdownMenu = element.nextElementSibling;
            // fetch ingredients from server
            const ingredientListResponse = await window.fetch("/Ingredients/All");

            if (!ingredientListResponse.ok) {
                // todo: add user friendly error
                throw new Error("Failed to retrieve ingredient list.")
            }
            // get json from API and set cachedIngredients
            const result = await ingredientListResponse.json();
            cachedIngredients = result;
            // populate dropdown
            await populateDropdown(element);
            // the dropdown should already be populated, so sever connection to this method
            return;
        }

        // Initialize labels on page load
        updateLabels();
    </script>
}